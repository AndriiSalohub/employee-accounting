import { AppInfo } from "../../components/AppInfo/AppInfo";
import "./App.css";

export const App = () => {
    return (
        <div className="app">
            <AppInfo />
        </div>
    );
};
